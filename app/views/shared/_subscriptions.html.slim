- if resource.subscribed?(current_user)
  = link_to 'Unsubscribe', unsubscribe_question_path(resource), remote: true, method: :delete
- elsif user_signed_in?
  = link_to 'Subscribe', subscribe_question_path(resource), remote: true, method: :post